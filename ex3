using System.Globalization;

class Ex3
{
    static void Main()
    {
        int[] vetor = new int[10];
        int maior,menor;

        Console.WriteLine("Digite 10 números: ");
        vetor[0]= int.Parse(Console.ReadLine());
        maior= vetor[0];
        menor= vetor[0];
        for (int i = 1; i < 10; i++)
        {
            Console.WriteLine("Informe um número: ");
            vetor[i] = int.Parse(Console.ReadLine());
            if (vetor[i]> maior)
            {maior = vetor[i];}
            if(vetor[i]< menor)
            {menor = vetor[i];}

        } Console.WriteLine($"O valor menor é {menor} e o valor maior é {maior}");
        }}